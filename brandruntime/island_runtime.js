// island_runtime.js — companion JS for the brandruntime .gsx islands.
//
// Stub placeholder for Section A.2. Real implementation lands in Section
// A.6. The stub exists so the //go:embed directive in runtime.go has a file
// to read at package compile time.
;(function () {
  if (typeof window === "undefined") return;
})();
